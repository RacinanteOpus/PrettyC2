function sortTable(myTable, n) {
  var table, rows, switching, i, x, y, shouldSwitch, dir, y1, switchcount = 0;
  table = document.getElementById(myTable);
  switching = true;
  dir = "asc";
  while (switching) {
    switching = false;
    rows = table.rows;
    /* Loop through all table rows except the table headers): */
    for (i = 1; i < (rows.length - 1); i++) {
      shouldSwitch = false;
      /* Compare current row and the next: */
      x = rows[i].getElementsByTagName("TD")[n];
      y = rows[i + 1].getElementsByTagName("TD")[n];
      y1 = rows[i + 1].getElementsByTagName("TD")[1];
      /* Check if the two rows should switch place,
      based on the direction, asc or desc: */
      if (dir == "asc") {
        if (x.innerHTML.toLowerCase() > y.innerHTML.toLowerCase()) {
          shouldSwitch = true;
          if (y1.innerHTML == "") { shouldSwitch = false; }
          break;
        }
      } else if (dir == "desc") {
        if (x.innerHTML.toLowerCase() < y.innerHTML.toLowerCase()) {
          shouldSwitch = true;
          if (y1.innerHTML == "") { shouldSwitch = false; }
          break;
        }
      }
    }
    if (shouldSwitch) {
      rows[i].parentNode.insertBefore(rows[i + 1], rows[i]);
      switching = true;
      switchcount ++;
    } else {
      /* If no switching has been done AND the direction is "asc",
      set the direction to "desc" and run the while loop again. */
      if (switchcount == 0 && dir == "asc") {
        dir = "desc";
        switching = true;
      }
    }
  }
}

function sortTable_num(myTable, n) {
  var table, rows, switching, i, x, y, y1, shouldSwitch, dir, switchcount = 0;
  table = document.getElementById(myTable);
  switching = true;
  dir = "asc";
  while (switching) {
    switching = false;
    rows = table.rows;
    /* Loop through all table rows except the table headers): */
    for (i = 1; i < (rows.length - 1); i++) {
      shouldSwitch = false;
      /* Compare current row and the next: */
      x = rows[i].getElementsByTagName("TD")[n];
      y = rows[i + 1].getElementsByTagName("TD")[n];
      y1 = rows[i + 1].getElementsByTagName("TD")[1];
      /* Check if the two rows should switch place,
      based on the direction, asc or desc: */
      xStr = x.innerHTML.replace(/[%,]/g, "");;
      yStr = y.innerHTML.replace(/[%,]/g, "");;
      if (dir == "asc") {
        if (Number(xStr) > Number(yStr)) {
          shouldSwitch = true;
          if (y1.innerHTML == "") { shouldSwitch = false; }
          break;
        }
      } else if (dir == "desc") {
        if (Number(xStr) < Number(yStr)) {
          shouldSwitch = true;
          if (y1.innerHTML == "") { shouldSwitch = false; }
          break;
        }
      }
    }
    if (shouldSwitch) {
      rows[i].parentNode.insertBefore(rows[i + 1], rows[i]);
      switching = true;
      switchcount ++;
    } else {
      /* If no switching has been done AND the direction is "asc",
      set the direction to "desc" and run the while loop again. */
      if (switchcount == 0 && dir == "asc") {
        dir = "desc";
        switching = true;
      }
    }
  }
}
